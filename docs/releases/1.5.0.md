# Veridra 1.5.0

- Adds an optional saved report-profile selector to the assessment console.
- Preserves the selected profile through assessments, area/status filters, reports, and evidence exports.
- Keeps default Veridra behavior unchanged when no profile is selected.
- Returns a controlled error for missing saved profiles.
